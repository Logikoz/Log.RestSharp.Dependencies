namespace Dependencies
{
    public struct Protocols
    {
        public const string HTTP = nameof(HTTP);
        public const string HTTPS = nameof(HTTPS);
        public const string FTP = nameof(FTP);
        public const string FTPS = nameof(FTPS);
        public const string SMTP = nameof(SMTP);
        public const string SSH = nameof(SSH);
    }
}